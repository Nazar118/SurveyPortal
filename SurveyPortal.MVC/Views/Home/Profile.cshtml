@{
    ViewData["Title"] = "Kullanıcı Paneli";
    Layout = "~/Views/Shared/_UserLayout.cshtml";
}

<style>
    /* 🔥 PORTAL (DASHBOARD) TASARIMI İÇİN ÖZEL CSS */
    .portal-sidebar {
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.03);
        height: 100%;
        min-height: 70vh;
    }

    .portal-avatar {
        width: 90px;
        height: 90px;
        font-size: 3rem;
        display: inline-flex;
        align-items: center;
        justify-content: center;
        border-radius: 50%;
        background: linear-gradient(45deg, #b66dff, #d8a7ff);
        color: white;
        box-shadow: 0 4px 10px rgba(182, 109, 255, 0.3);
    }

    /* Sol Menü Butonları */
    .nav-pills .nav-link {
        color: #444;
        border-radius: 10px;
        padding: 14px 20px;
        font-weight: 500;
        margin-bottom: 8px;
        transition: all 0.3s ease;
        display: flex;
        align-items: center;
    }

        .nav-pills .nav-link i {
            font-size: 1.2rem;
            margin-right: 12px;
            color: #888;
            transition: all 0.3s ease;
        }

        .nav-pills .nav-link:hover:not(.active) {
            background-color: #f8f9fc;
            color: #b66dff;
            transform: translateX(5px);
        }

            .nav-pills .nav-link:hover:not(.active) i {
                color: #b66dff;
            }

        .nav-pills .nav-link.active {
            background: linear-gradient(to right, #b66dff, #9834ff);
            color: white !important;
            box-shadow: 0 4px 10px rgba(182, 109, 255, 0.4);
        }

            .nav-pills .nav-link.active i {
                color: white;
            }

    /* Sağ İçerik Alanı */
    .portal-content {
        background: #fff;
        border-radius: 12px;
        box-shadow: 0 4px 15px rgba(0,0,0,0.03);
        padding: 40px;
        min-height: 70vh;
    }

    /* Anket Listesi Hover Efekti */
    .survey-list-item {
        transition: all 0.2s ease;
        border-left: 4px solid transparent;
    }

        .survey-list-item:hover {
            background-color: #fcfcfc;
            border-left-color: #b66dff;
        }
</style>

<div class="container-fluid mt-4 mb-5" style="max-width: 1400px;">
    <div class="row g-4">

        <!-- 🔥 SOL MENÜ (SIDEBAR PORTAL ALANI) -->
        <div class="col-lg-3 col-md-4">
            <div class="portal-sidebar p-4">

                <!-- Profil Özeti -->
                <div class="text-center mb-4 pb-4 border-bottom">
                    <div class="portal-avatar mb-3">
                        <i class="mdi mdi-account"></i>
                    </div>
                    <h5 class="font-weight-bold text-dark mb-1" id="sideUsername">Yükleniyor...</h5>
                    <span class="badge badge-outline-primary badge-pill px-3 py-1" id="sideRole">...</span>
                </div>

                <!-- Menü (Nav Pills) -->
                <div class="nav flex-column nav-pills" id="v-pills-tab" role="tablist" aria-orientation="vertical">

                    <button class="nav-link active" id="v-pills-surveys-tab" data-bs-toggle="pill" data-bs-target="#v-pills-surveys" type="button" role="tab">
                        <i class="mdi mdi-format-list-checks"></i> Katıldığım Anketler
                    </button>

                    <button class="nav-link" id="v-pills-profile-tab" data-bs-toggle="pill" data-bs-target="#v-pills-profile" type="button" role="tab">
                        <i class="mdi mdi-card-account-details-outline"></i> Hesap Bilgilerim
                    </button>

                    <hr class="my-3">

                    <a href="/Home/Index" class="nav-link text-dark" style="background: transparent;">
                        <i class="mdi mdi-compass-outline"></i> Yeni Anket Keşfet
                    </a>

                    <button class="nav-link text-danger mt-2" onclick="logout()" style="background: transparent;">
                        <i class="mdi mdi-logout text-danger"></i> Güvenli Çıkış
                    </button>

                </div>
            </div>
        </div>

        <!-- 🔥 SAĞ İÇERİK ALANI (DINAMIK DEĞİŞEN ALAN) -->
        <div class="col-lg-9 col-md-8">
            <div class="portal-content">
                <div class="tab-content" id="v-pills-tabContent">

                    <!-- TAB 1: ANKETLERİM -->
                    <div class="tab-pane fade show active" id="v-pills-surveys" role="tabpanel">
                        <div class="d-flex justify-content-between align-items-center mb-4 pb-2 border-bottom">
                            <h3 class="font-weight-bold m-0"><i class="mdi mdi-check-network text-success me-2"></i> Anket Geçmişim</h3>
                        </div>

                        <div id="mySurveysList">
                            <div class="text-center py-5">
                                <div class="spinner-border text-primary" role="status"></div>
                                <p class="mt-3 text-muted font-weight-bold">Verileriniz getiriliyor...</p>
                            </div>
                        </div>
                    </div>

                    <!-- TAB 2: HESAP BİLGİLERİM -->
                    <div class="tab-pane fade" id="v-pills-profile" role="tabpanel">
                        <h3 class="font-weight-bold mb-4 pb-2 border-bottom"><i class="mdi mdi-shield-account-outline text-primary me-2"></i> Hesap Detayları</h3>

                        <div class="row">
                            <div class="col-md-6 mb-4">
                                <label class="text-muted font-weight-bold mb-2">Kullanıcı Adı</label>
                                <div class="form-control form-control-lg bg-light border-0" id="detailUsername">...</div>
                            </div>
                            <div class="col-md-6 mb-4">
                                <label class="text-muted font-weight-bold mb-2">E-Posta Adresi</label>
                                <div class="form-control form-control-lg bg-light border-0" id="detailEmail">...</div>
                            </div>
                            <div class="col-md-6 mb-4">
                                <label class="text-muted font-weight-bold mb-2">Yetki Seviyesi</label>
                                <div class="form-control form-control-lg bg-light border-0 text-primary font-weight-bold" id="detailRole">...</div>
                            </div>
                            <div class="col-md-6 mb-4">
                                <label class="text-muted font-weight-bold mb-2">Hesap Durumu</label>
                                <div class="form-control form-control-lg bg-light border-0 text-success font-weight-bold"><i class="mdi mdi-check-circle"></i> Aktif</div>
                            </div>
                        </div>
                    </div>

                </div>
            </div>
        </div>

    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {
            // Sayfa açılır açılmaz profili yükle
            loadUserProfile();

            // 🔥 TAB GEÇİŞLERİNİ GARANTİYE ALAN KOD (Bootstrap 4 & 5 Uyumlu)
            $(".nav-pills .nav-link").click(function () {
                // Sadece çıkış veya ana sayfa butonu değilse çalışsın
                if($(this).attr("id")) {
                    $(".nav-pills .nav-link").removeClass("active");
                    $(this).addClass("active");

                    var target = $(this).attr("data-bs-target") || $(this).attr("data-target");
                    $(".tab-pane").removeClass("show active");
                    $(target).addClass("show active");
                }
            });
        });

        function loadUserProfile() {
            var token = localStorage.getItem("token");
            if (!token) {
                window.location.href = "/Auth/Login";
                return;
            }

            $.ajax({
                url: "https://localhost:7283/api/user/me",
                type: "GET",
                headers: { "Authorization": "Bearer " + token },
                success: function (data) {
                    $("#sideUsername").text(data.username);
                    $("#sideRole").text(data.role);

                    $("#detailUsername").text(data.username);
                    $("#detailEmail").text(data.email);
                    $("#detailRole").text(data.role);

                    var listContainer = $("#mySurveysList");
                    listContainer.empty();

                    if (data.participatedSurveys.length === 0) {
                        listContainer.html(`
                            <div class="text-center py-5">
                                <i class="mdi mdi-text-box-search-outline text-muted" style="font-size: 5rem;"></i>
                                <h4 class="mt-3 text-muted">Burası biraz ıssız...</h4>
                                <p class="text-muted">Henüz hiçbir ankete katılmadınız. Fikirlerinizi paylaşmaya hemen başlayın!</p>
                                <a href="/Home/Index" class="btn btn-gradient-primary mt-2 px-4 rounded-pill">Anketleri Keşfet</a>
                            </div>
                        `);
                        return;
                    }

                    $.each(data.participatedSurveys, function (index, survey) {
                        var date = new Date(survey.completedDate).toLocaleDateString("tr-TR", {
                            year: 'numeric', month: 'long', day: 'numeric', hour: '2-digit', minute:'2-digit'
                        });

                        var item = `
                            <div class="survey-list-item d-flex justify-content-between align-items-center py-4 px-3 border-bottom rounded">
                                <div>
                                    <h5 class="font-weight-bold mb-1 text-dark">${survey.surveyTitle}</h5>
                                    <span class="text-muted" style="font-size: 0.9rem;"><i class="mdi mdi-calendar-check text-success me-1"></i> Katılım Tarihi: ${date}</span>
                                </div>
                                <div>
                                    <a href="/Home/UserResult/${survey.surveyId}" class="btn btn-outline-primary rounded-pill px-4 shadow-sm font-weight-bold">
                                        Sonuçları Gör <i class="mdi mdi-arrow-right ms-1"></i>
                                    </a>
                                </div>
                            </div>
                        `;
                        listContainer.append(item);
                    });
                },
                error: function (xhr) {
                    Swal.fire({
                        icon: 'error',
                        title: 'Oturum Hatası',
                        text: 'Profil bilgilerinizi çekerken bir sorun oluştu. Lütfen tekrar giriş yapın.'
                    }).then(() => {
                        logout();
                    });
                }
            });
        }

        function logout() {
            localStorage.removeItem("token");
            localStorage.removeItem("userRole");
            window.location.href = "/Auth/Login";
        }
    </script>
}